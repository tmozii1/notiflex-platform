# AGENTS.md - Notiflex 인프라 실습

## 프로젝트 개요

이 저장소는 책 실습을 진행하기 위한 Notiflex 인프라 구성/배포 프로젝트다.

- 책 목차와 진행 상황: `/Users/tim/project/timob/300_Book/310_AI/인프라 구성배포/목차.md`
- 실습 프로젝트 경로: `/Users/tim/project/gcp`
- 시나리오: B2B SaaS 알림 플랫폼 `Notiflex`
- 사용자 역할: Codex와 함께 Notiflex 인프라와 서비스를 관리하는 DevOps 엔지니어

Notiflex는 고객사의 서비스에서 발생하는 회원가입, 결제, 배송 같은 이벤트를 API로 받아 이메일, SMS, Push Alarm으로 발송한다. 고객사는 API만 호출하고, 발송 처리와 배포, 관측 가능성, 운영은 Notiflex가 책임진다.

## 기준 문서

Obsidian의 책 목차 문서를 학습 로드맵과 진행 상황의 기준으로 사용한다.

```text
/Users/tim/project/timob/300_Book/310_AI/인프라 구성배포/목차.md
```

새 장이나 절을 시작하기 전에 해당 파일에서 다음 내용을 확인한다.

- 현재 체크/미체크 진행 상황
- Notiflex 시나리오 맥락
- 장별 목표와 제약 조건
- 사용자가 학습 중 추가한 메모

구현과 실습 중 생성되는 프로젝트 문서는 이 저장소의 `docs/` 아래에 둔다. 프로젝트 문서를 임의의 다른 폴더에 흩어두지 않는다.

## 학습과 실행 방식

이 프로젝트는 학습용 실습이므로 설명 후 실행 흐름을 선호한다.

1. 현재 상태를 짧게 탐색한다.
2. 다음 구체적인 단계를 한국어로 설명한다.
3. 명령 실행 또는 파일 수정을 진행한다.
4. 결과를 검증한다.
5. 의미 있는 결과물은 `docs/`에 기록한다.

설명은 실습에 필요한 만큼만 구체적으로 작성한다. 사용자가 요청하지 않는 한 긴 이론 정리보다 직접 만들면서 배우는 흐름을 우선한다.

## 현재 로드맵

학습은 Notiflex의 성장 타임라인을 따른다.

- 2장: 첫 Notiflex API 서버를 만들고 GKE에 배포한다.
- 3장: ArgoCD로 GitOps를 도입한다.
- 4장: 메트릭, 로그, 알림으로 장애 대응을 위한 관측 가능성을 구축한다.
- 5장: 무중단 배포로 전환한다.
- 6장: 캐시, 시크릿 관리, 배포 전략으로 엔터프라이즈 기반을 정비한다.
- 7장: 인프라를 확장하고 테넌트를 분리한다.
- 8장: 이벤트 드리븐 구조, 분산 트레이싱, 배치 자동화를 도입한다.
- 9장: 저장소를 GitAIOps 운영 표준으로 정리한다.

## 예상 저장소 구조

이후 장에서 의도적으로 바꾸기 전까지 다음 구조를 기준으로 삼는다.

```text
.
├── AGENTS.md
├── README.md
├── docs/
│   ├── ch02/
│   ├── ch03/
│   └── ch04/
├── app/
├── deploy/
│   ├── k8s/
│   └── argocd/
├── infra/
└── scripts/
```

폴더는 실제로 필요해질 때 만든다. 단, `docs/`는 프로젝트 문서의 기준 위치이므로 유지한다.

## 문서 작성 규칙

프로젝트 관련 문서는 `docs/`를 사용한다.

- 문서는 `architecture`, `decisions`, `notes` 같은 유형별 폴더로 나누지 않는다.
- 장별 폴더를 기준으로 정리한다. 예: `docs/ch02/`, `docs/ch03/`, `docs/ch04/`
- 각 장에서 만든 설계, 결정 기록, 학습 노트, 운영 절차는 모두 해당 장 폴더에 둔다.
- 장을 마무리할 때 해당 장 폴더 안의 신규 문서와 변경 문서를 확인하고 `README.md`, `JOURNEY.md` 등 루트 문서의 참조도 함께 갱신한다.
- Obsidian 속성과 문서 간 wikilink는 `docs/` 아래의 실습 문서와 `JOURNEY.md`에만 적용한다.
- `README.md`는 GitHub 공개 화면용 문서이므로 Obsidian YAML 속성, Obsidian 전용 연결 섹션, wikilink를 넣지 않는다.
- `AGENTS.md`는 프로젝트 지침 파일이므로 Obsidian YAML 속성이나 wikilink를 넣지 않는다.
- 새 실습 Markdown 문서를 만들 때는 상단에 YAML 속성을 작성하고, 본문 초반에 관련 문서 연결 섹션을 둔다.
- 기본 속성은 제목, 별칭, 장 번호, 문서 유형, 상태, 생성일, 수정일, 태그, 관련 문서를 포함한다.
- 관련 문서에는 직전/다음 문서, 상위 진행 문서, 직접 관련된 설계/결정/구현 문서를 넣는다.

문서 파일명은 생성 순서를 알 수 있도록 전역 증가 번호를 앞에 붙인다.

- 첫 번째 문서: `01_...md`
- 두 번째 문서: `02_...md`
- 세 번째 문서: `03_...md`

번호는 `docs/` 전체를 기준으로 이어간다. 하위 폴더별로 다시 `01_`부터 시작하지 않는다. 새 문서를 만들기 전에는 `find docs -type f` 등으로 가장 큰 번호를 확인하고 다음 번호를 사용한다.

예시:

```text
docs/ch02/01_gcp-environment.md
docs/ch02/05_notiflex-app-design.md
docs/ch03/07_argocd-installation.md
```

작업 중 운영 지식이 생기면 문서를 새로 만들거나 갱신한다. 예시는 다음과 같다.

- GCP 프로젝트와 리전 선택 이유
- GKE 클러스터 생성 파라미터
- Artifact Registry 이름 규칙
- Kubernetes 네임스페이스와 매니페스트 규칙
- ArgoCD 애플리케이션 구조
- 모니터링, 로깅, 알림, 장애 대응 절차

시크릿, 토큰, 인증 정보, kubeconfig 파일, 서비스 계정 키는 `docs/`나 git에 저장하지 않는다.

## 명령 실행 안전 규칙

클라우드와 Kubernetes 작업에서는 다음 규칙을 따른다.

- 파괴적이거나 비용에 영향을 줄 수 있는 작업은 실행 전에 정확한 명령을 보여준다.
- GCP/GKE 변경 전 활성 계정, 프로젝트, 리전, 클러스터 컨텍스트를 확인한다.
- 가능하면 `dry-run`, `describe`, `diff`, `plan` 성격의 명령을 먼저 사용한다.
- 사용자가 명시적으로 요청하지 않는 한 클라우드 리소스, 네임스페이스, 클러스터, 디스크, 레지스트리, 시크릿을 삭제하지 않는다.
- `delete`, `destroy`, `rm`, `reset`, 인증 정보 변경이 포함된 명령은 고위험 작업으로 다룬다.

비용이 발생할 수 있는 GCP 명령을 실행하기 전에는 다음을 확인한다.

```bash
gcloud auth list
gcloud config list
```

Kubernetes 변경 전에는 다음을 확인한다.

```bash
kubectl config current-context
kubectl get nodes
```

## 코드 작성 규칙

현재 진행 중인 장에서 도입한 규칙을 따른다. 앱 스택이 만들어지기 전까지는 다음을 기본으로 한다.

- 소스 파일은 작고 읽기 쉽게 유지한다.
- 과한 추상화보다 명확한 이름을 우선한다.
- 오류 처리는 명시적으로 작성한다.
- 실패할 수 있는 동작은 최소한의 테스트나 런타임 검증을 추가한다.
- 실습에 꼭 필요한 파일이 아니라면 생성물이나 도구 전용 파일은 git에 넣지 않는다.

2장에서 예정한 대로 앱을 Go로 구현한다면 다음을 따른다.

- `gofmt`를 사용한다.
- 인프라 복잡도를 더하기 전에 API 서버를 단순하게 유지한다.
- Kubernetes 배포 시 health/readiness 엔드포인트를 제공한다.
- 설정은 환경 변수를 우선 사용한다.

## Git과 진행 상황

이 저장소는 git 없이 시작할 수 있다. git을 초기화한 뒤에는 다음을 따른다.

- 커밋은 책의 절 또는 의미 있는 실습 단위에 맞춘다.
- 커밋 전 관련 검증 명령을 실행한다.
- 시크릿, `.env`, kubeconfig, 로컬 인증 파일은 커밋하지 않는다.
- 커밋 메시지에는 장 또는 기능이 드러나게 작성한다.

진행 상황은 Obsidian 목차 파일에서 관리한다. 저장소 바깥의 문서를 수정해야 하면, 권한이 필요한 경우 사용자에게 확인한다.

## bkit / PDCA 참고

bkit은 전역으로 설치되어 있으며 PDCA 흐름을 제공할 수 있다. 구현 작업에서는 다음을 따른다.

- 큰 작업 전 현재 상태를 확인한다.
- 설계 문서가 있으면 해당 문서를 기준으로 구현한다.
- 새 프로젝트 문서는 `docs/`에 둔다.
- 큰 기능에 대한 설계 문서가 없으면 구현 전에 설계 문서 작성을 제안한다.

작은 학습 단계와 환경 구성은 흐름을 가볍게 유지하되, 중요한 결정은 문서로 남긴다.

## 어시스턴트 동작 규칙

사용자가 달리 요청하지 않는 한 사용자-facing 설명은 한국어로 작성한다. 진행 상황 업데이트는 간결하고 구체적으로 한다. 사용자가 특정 장이나 절을 진행하자고 하면 먼저 Obsidian 목차를 확인하고, 그다음 저장소 상태를 확인한 뒤 실행한다.

다음 흐름을 선호한다.

```text
탐색 -> 비교/판단 -> 실행 -> 검증 -> 기록
```

클라우드 상태는 추측하지 말고 확인한다.
