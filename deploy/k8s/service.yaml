apiVersion: v1
kind: Service
metadata:
  name: notiflex-api
  namespace: notiflex
  labels:
    app.kubernetes.io/name: notiflex-api
    app.kubernetes.io/part-of: notiflex
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: notiflex-api
  ports:
    - name: http
      port: 80
      targetPort: http
